import { FilterProps } from '@/lib/components/shared/filter/filter.type'

const Filter = ({ id, name, type, values }: FilterProps) => {
  return (
    <ul>
      <li></li>
    </ul>
  )
}

export default Filter
